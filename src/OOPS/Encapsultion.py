# Encapsultion: Wrapping the data(variables) and the methods(functions) that operate into a single unit(Class).

# It also restrict direct access to some variables --> to protect data

# Focus: How the data is accessed / Modified safely.